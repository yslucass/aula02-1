import { useEffect, useState } from "react";

export default function App() {
  
  const [??,??] = useState([]);

  useEffect(() => { 

    const buscarUsuario  = async () => {
                    = await fetch('');
        const dados =                ;
        setUsuario(dados);
    }
    buscarUsuario();
  }, //complete o código);

  return (
    <>
      <h1>Usuário</h1>
      <ul>
        {}
      </ul>
    </>
  );
}
